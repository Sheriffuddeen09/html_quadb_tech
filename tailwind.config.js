/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./*.html'],
    darkMode: 'class', // Enables dark mode via the 'dark' class
    theme: {
      extend: {},
    },
    plugins: [],
  };
